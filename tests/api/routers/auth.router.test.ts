import request from 'supertest';
import { authRouter } from '../../../src/api/routers';

describe('Tests form authRouter /api/v1/auth', () => {
  it('should return 401 if GET /auth/login', () => {});
});
